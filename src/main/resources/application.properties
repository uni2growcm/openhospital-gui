## OpenHospital GUI Spring Boot properties
# Allow circular references temporarily to work around bean dependency cycle in OH-core
# TODO: remove this when refactoring beans to eliminate the cycle
spring.main.allow-circular-references=true

# You can add other application-specific properties here

